{
  "name": "cf-backend",
  "scripts": {
    "dev": "wrangler dev --env dev --experimental-vectorize-bind-to-prod --remote",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0"
  }
}
