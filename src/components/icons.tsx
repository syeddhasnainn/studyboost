import { LucideProps, FileText, Brain, Target, Command, Check, } from "lucide-react"

export const Icons = {
  logo: Command,
  document: FileText,
  brain: Brain,
  target: Target,
  check: Check,
} 